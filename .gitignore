# FB session — contains auth cookies, NEVER commit
fb-profile/

# scraped data + artifacts
raw.json
listings.db
images/
groups.txt          # your real target groups — keep private (groups.txt.example is tracked)
skills-lock.json    # from the `skills` installer

# secrets
.env

# python
__pycache__/
*.pyc
.venv/

# local-only design docs — never commit
docs/

# third-party skills (installed locally, not part of the repo)
.agents/
