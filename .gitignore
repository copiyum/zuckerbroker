# FB session — contains auth cookies, NEVER commit
fb-profile/

# scraped data + artifacts
raw.json
listings.db
images/

# secrets
.env

# python
__pycache__/
*.pyc
.venv/

# local-only design docs — never commit
docs/

# third-party skills (installed locally, not part of the repo)
.agents/
